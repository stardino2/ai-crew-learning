# Harness Engineering Slides

## Preferred

Install dependencies and run the Slidev deck:

```powershell
npm install
npm run dev
```

## Backup

Open `standalone.html` directly in a browser. Use left/right arrow keys to move between slides.

## Notes

Speaker notes are embedded in `slides.md` as HTML comments under each slide.

